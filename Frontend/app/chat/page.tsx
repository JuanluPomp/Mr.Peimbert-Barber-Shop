"use client"

// Página del chatbot — CSR, conecta al backend en fase posterior

export default function ChatPage() {
  return (
    <div className="min-h-screen bg-[#0a0a0a] flex items-center justify-center">
      <p className="text-[#A0A0A0] font-sans">Chat — próximamente</p>
    </div>
  )
}
